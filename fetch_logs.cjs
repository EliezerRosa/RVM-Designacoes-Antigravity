const { createClient } = require("@supabase/supabase-js"); const fs = require("fs"); const envContent = fs.readFileSync(".env", "utf8"); let url="", key=""; for(let line of envContent.split("\n")) { if(line.startsWith("VITE_SUPABASE_URL=")) url = line.split("=")[1].trim(); if(line.startsWith("VITE_SUPABASE_SERVICE_ROLE_KEY=")) key = line.split("=")[1].trim(); } const supabase = createClient(url, key); async function run() { const { data } = await supabase.from("zapi_smart_interactions").select("*").order("created_at", { ascending: false }).limit(5); for (let r of data) console.log(`[${r.created_at}] ${r.phone} | Intent: ${r.detected_intent} | Action: ${r.action_taken} | Reply: ${r.outbound_reply_text}`); } run();
