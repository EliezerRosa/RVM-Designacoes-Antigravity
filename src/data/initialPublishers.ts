import type { Publisher } from '../types';

// O arquivo original com dados reais foi movido para dados_sensiveis/initialPublishers_REAL.ts
// Este arquivo permanece vazio para garantir que o código compile sem expor dados pessoais no GitHub.
// Se precisar reseeding, use o backup ou restaure o arquivo da pasta segura.

export const initialPublishers: Publisher[] = [];
